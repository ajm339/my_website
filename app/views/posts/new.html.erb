<% provide(:title, 'New Post') %> 
<div class="padding">
	<div class="row">
		<div class="large-8 large-offset-2 columns">
			<h4>New post</h4>

			<%= render 'form' %>

			<%= link_to 'Back', blog_path %>
		</div>
	</div>
</div>